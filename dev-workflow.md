# 独立开发者工作流（个人方法论）

> **性质：** 方法论 / 流程笔记（非某篇官方文档的技术笔记）。
> **适用：** Mini CRM 转型计划期间，以及未来海外 freelancer 接单时的个人标准工作流。
> **一句话：** 从使用者需求出发 → 评审需求 → 先定测试用例 → 形成故事 → （复杂则设计评审/选型）→ 开发 → 测试 → 上生产验证，故事才算完成。

## 核心理念

- **从使用者、业务场景出发**，不从功能列表出发。功能列表会遗漏"使用者角度才看得到"的隐藏需求。
- **测试先行**：开发前先定测试用例。用例是"完成"的客观标准，不靠主观"我觉得好了"。
- **YAGNI（你需要时才做）**：需求评审时默认砍，砍不掉再留。独立开发最大的浪费是做了不该做的。
- **故事可追溯**：故事文档落工程 `docs/stories/`，提交代码时备注实现了哪个故事。

## 完整流程

### 1. 需求洞察
- 站在使用者角度：用户需要什么功能、达到什么目的？
- 判断需求是否**合理、通用**（是个例需求还是普适需求）。
- 主动追问"这一版能不能不做"，先砍后留（YAGNI）。

### 2. 需求文档（粗）
- 把想清楚的需求粗略写成需求文档，不求完备，求方向对。

### 3. 需求评审
- 与 AI 探讨需求：是否合理、是否有遗漏、边界在哪、能否缩小范围。

### 4. 测试用例
- 产出测试用例（即验收标准）。覆盖正常路径 + 关键异常/边界。
- 此步即 TDD/BDD 思路：先定"怎么算对"，再写代码。

### 5. 用例评审
- 与 AI 讨论用例，找出不正确、不完整、冗余的用例并修正。

### 6. 形成故事
- 需求 + 测试用例 → **故事文档**，放工程 `docs/stories/`（每个故事一个文件）。
- 故事用"作为 <角色>，我想 <功能>，以便 <目的>"格式描述。
- 提交代码时在 commit 备注实现了哪个故事（故事号/文件名）。

### 7. 设计评审 + 技术选型（仅复杂功能）
- 设计具体实现方案，预判可能遇到的坑。
- 若引入项目中没有的技术，先做技术选型再开发。
- 简单功能可跳过此步。

### 8. 开发
- 按设计实现，对齐故事里的需求与用例。

### 9. 测试
- 按测试用例逐条验证，用例全过才算开发完成（不靠主观判断）。

### 10. 测试环境 → 生产环境验证
- 测试环境通过后，部署到生产再测一轮。
- 生产验证通过，故事才算 **完成**。

## 完成定义（DoD）

一个故事满足以下全部，才算完成：

- [ ] 测试用例全部通过
- [ ] 代码已提交，commit 备注了对应故事号
- [ ] 代码已合并到主干
- [ ] 已部署测试环境并验证通过
- [ ] 已部署生产环境并验证通过

## 与 Mini CRM 计划的关系

- 计划文档（`docs/superpowers/plans/`）是**大纲**，实际推进**以需求为准**。计划的每日清单是参考颗粒度，真实接口/DTO 由故事驱动倒推。
- 故事文档落 `mini-crm/docs/stories/`，是介于"计划"和"代码"之间的可追溯单元。

## 术语表

| 英文 | 释义 |
|------|------|
| User Story | 用户故事，"作为<角色>我想<功能>以便<目的>"格式描述需求 |
| TDD / BDD | 测试驱动 / 行为驱动开发，先写用例（验收标准）再写实现 |
| YAGNI | You Aren't Gonna Need It，不为假想的未来需求提前开发 |
| DoD | Definition of Done，完成定义，判断工作是否真正完成的客观清单 |
| Acceptance Criteria | 验收标准，故事达成与否的可验证条件，常以测试用例形式表达 |
